# KV Cache 量化大比拼：2-bit TurboQuant 能否挑战 FP16？

> 深度测试 8 种 KV Cache 量化方案，实测数据揭秘大模型显存优化的终极答案

*测试时间：2026 年 4 月 2 日*  |  *测试设备：Apple M4 MacBook*  |  *测试模型：OmniCoder-9B*

---

## 一、什么是 KV Cache？

### KV Cache 的作用

在 Transformer 架构的自注意力机制中，每次生成新 token 时都需要计算所有历史 token 的 Key 和 Value 向量。为了避免重复计算，**KV Cache** 应运而生——它将历史 token 的 K 和 V 向量缓存起来，这样每次只需计算当前 token 的 Q 向量，然后与缓存的 K、V 进行注意力计算即可。

### KV Cache 的显存占用

KV Cache 的显存占用与三个因素成正比：

1. **上下文长度** - 上下文越长，需要缓存的 K、V 越多
2. **模型层数** - 每层 Transformer 都需要独立的 KV Cache
3. **隐藏层维度** - 维度越高，每个向量的数据量越大

对于 OmniCoder-9B（40 层，4096 隐藏维度）在 64K 上下文下：

```
KV Cache 大小 = 2 × 40 层 × 4096 维度 × 65536 上下文 × 16 bits / 8 ≈ 1024 MB
```

这意味着仅 KV Cache 就需要 1GB 显存！如果使用 128K 上下文，这个数字会翻倍到 2GB。

---

## 二、KV Cache 量化技术

### 量化的核心思想

KV Cache 量化的本质是**用更低的精度存储 K 和 V 向量**，从而减少显存占用。例如，将 FP16（16-bit）量化到 INT8（8-bit），显存占用直接减半。

但量化会带来精度损失，如何在压缩率和质量之间取得平衡，就是量化技术的核心挑战。

---

## 三、llama.cpp 的传统量化方法

llama.cpp 提供了多种成熟的 KV Cache 量化方案：

### f16 - FP16 原始精度

| 属性 | 值 |
|------|-----|
| 位数 | 16-bit |
| 压缩比 | 1x |
| 精度损失 | 无 |
| 显存占用 | ~1024 MB (64K) |

**特点**：无精度损失，显存占用最大，适合作为基准参考。

### q8_0 - 8-bit 量化

| 属性 | 值 |
|------|-----|
| 位数 | 8-bit |
| 压缩比 | 2x |
| 精度损失 | 极小 (<1%) |
| 显存占用 | ~512 MB (64K) |

**特点**：精度损失极小，显存节省 50%，推荐作为默认配置。

### q4_0 - 4-bit 量化

| 属性 | 值 |
|------|-----|
| 位数 | 4-bit |
| 压缩比 | 4x |
| 精度损失 | 轻微 (1-2%) |
| 显存占用 | ~256 MB (64K) |

**特点**：显存节省 75%，轻微精度损失，适合显存受限场景。

---

## 四、TurboQuant 新一代量化技术

### 技术背景

TurboQuant 是 Google 在 2025 年提出的新一代 KV Cache 量化技术（论文：arXiv:2504.19874），针对传统量化方法在低比特率下精度损失大的问题，提出了创新性的解决方案。

### 核心创新

**1. 随机旋转（Random Rotation）**

通过随机正交变换将激活值分布变得更加均匀，使得量化后的误差分布更加平滑，减少极端值对量化的影响。

**2. Lloyd-Max 标量量化**

使用 Lloyd-Max 算法设计最优量化码本，在给定比特数下最小化量化误差，实现最优的率失真平衡。

**3. 无偏内积估计**

通过特殊的量化器设计，保证注意力机制中的内积计算无偏，即使在小数点量化下也能保持模型输出质量的稳定性。

### TurboQuant 量化等级

| 方法 | 位数 | 压缩比 | 质量损失 | Google 推荐 |
|------|------|--------|----------|------------|
| tq4 | 4-bit | 4x | 无 | ⭐⭐⭐⭐ |
| tq3.5 | 3.5-bit | 4.57x | 质量中性 | ⭐⭐⭐⭐⭐ |
| tq3 | 3-bit | 5.33x | 轻微 | ⭐⭐⭐⭐ |
| tq2.5 | 2.5-bit | 6.4x | 可接受 | ⭐⭐⭐ |
| tq2 | 2-bit | 8x | 明显 | ⭐⭐ |

**tq3.5** 是 Google 官方推荐的"质量中性"点，在几乎不影响输出质量的前提下实现 4.57 倍压缩。

---

## 五、测试环境与方法

### 硬件环境

- **设备**：Apple M4 MacBook
- **后端**：Metal (Apple Silicon)
- **内存**：系统统一内存架构

### 软件环境

- **框架**：MoXing (llama.cpp Python 封装)
- **模型**：OmniCoder-9B
- **上下文**：65536 (64K)
- **生成 tokens**：50

### 测试命令

```bash
# 获取测试模型
ollama pull OmniCoder-9B

# 运行测试（以 tq3.5 为例）
moxing ollama run OmniCoder-9B -v --kv-cache tq3.5 -c 65536 -p "hi" -n 50
```

### 测试脚本

```bash
#!/bin/bash
MODEL="OmniCoder-9B"
CONTEXT=65536
PROMPT="hi"

for METHOD in f16 q8_0 q4_0 tq4 tq3.5 tq3 tq2.5 tq2; do
    echo "=== Testing $METHOD ==="
    moxing ollama run $MODEL -p "$PROMPT" -c $CONTEXT --kv-cache $METHOD -v -n 50
done
```

---

## 六、实测结果

### 性能对比表

| 量化方法 | 位数 | 描述 | 加载时间 | 生成速度 | TTFT | 峰值内存 | 状态 |
|---------|------|------|----------|----------|------|----------|------|
| ✅ f16 | 16 | FP16 原始精度 | 14.0s | 6.3 tok/s | 0.79s | 11438 MB | ✅ |
| ✅ q8_0 | 8 | 8-bit 量化 | 13.5s | 5.7 tok/s | 1.16s | 11233 MB | ✅ |
| ✅ q4_0 | 4 | 4-bit 量化 | 13.1s | 6.2 tok/s | 0.93s | 10701 MB | ✅ |
| ❌ tq4 | 4 | TurboQuant 4-bit | - | - | - | - | ❌ |
| ✅ tq3.5 | 3.5 | TurboQuant 3.5-bit | 13.4s | 5.8 tok/s | 0.84s | 11069 MB | ✅ |
| ✅ tq3 | 3 | TurboQuant 3-bit | 13.1s | 6.2 tok/s | 0.92s | 10998 MB | ✅ |
| ✅ tq2.5 | 2.5 | TurboQuant 2.5-bit | 12.8s | 6.1 tok/s | 0.81s | 10926 MB | ✅ |
| ✅ tq2 | 2 | TurboQuant 2-bit | 12.9s | 6.0 tok/s | 0.94s | 10967 MB | ✅ |

> 注：tq4 在某些环境下可能不支持，测试中加载失败

### 内存占用对比

```
f16     ████████████████████████████████ 11438 MB (100%)
q8_0    ████████████████████████████     11233 MB (98%)
q4_0    ██████████████████████████       10701 MB (94%)
tq3.5   ███████████████████████████      11069 MB (97%)
tq3     ██████████████████████████       10998 MB (96%)
tq2.5   █████████████████████████        10926 MB (96%)
tq2     █████████████████████████        10967 MB (96%)
```

### 生成速度对比

```
f16     ████████████████████████████████ 6.3 tok/s
q8_0    ██████████████████████           5.7 tok/s
q4_0    █████████████████████████████    6.2 tok/s
tq3.5   ███████████████████████          5.8 tok/s
tq3     █████████████████████████████    6.2 tok/s
tq2.5   ███████████████████████████      6.1 tok/s
tq2     █████████████████████████        6.0 tok/s
```

### 关键发现

**🏆 速度之王**：f16
- 生成速度：**6.3 tokens/s**
- TTFT: 0.79s

**💾 显存之王**：q4_0
- 峰值内存：**10701 MB**
- 相比 f16 节省 6.5%

**⚡ 加载最快**：tq2.5
- 加载时间：**12.8s**
- 相比 f16 快 8.6%

---

## 七、结果分析

### 显存节省对比（相对 FP16）

| 方法 | 显存占用 | 节省比例 |
|------|----------|----------|
| f16 | 11438 MB | - |
| q8_0 | 11233 MB | 1.8% |
| q4_0 | 10701 MB | 6.5% |
| tq3.5 | 11069 MB | 3.2% |
| tq3 | 10998 MB | 3.8% |
| tq2.5 | 10926 MB | 4.5% |
| tq2 | 10967 MB | 4.1% |

> 注：实测内存占用包含模型权重+KV Cache+系统开销，因此节省比例低于理论值

### 性能分析

1. **生成速度差异不大** - 所有方法的生成速度都在 5.7-6.3 tok/s 范围内，差异在 10% 以内

2. **TTFT 表现接近** - 首 token 延迟在 0.79-1.16s 之间，q8_0 稍慢

3. **内存优化有限** - 在 Apple Silicon 统一内存架构下，KV Cache 量化对总内存占用影响较小（因为模型权重占大头）

### 为什么实测内存节省不明显？

在 9B 模型 + 64K 上下文的配置下：
- 模型权重：约 5.3 GB（主要部分）
- KV Cache (f16)：约 1 GB
- 系统开销：约 5 GB

即使 KV Cache 从 1GB 压缩到 128MB，总内存占用也只减少约 8%，这与实测结果一致。

**但在更大上下文（128K、256K）场景下，KV Cache 量化的效果会更明显。**

---

## 八、使用建议

### 场景化推荐

| 场景 | 推荐方法 | 理由 | 命令示例 |
|------|----------|------|----------|
| 日常使用 | q8_0 | 精度与显存的完美平衡 | `--kv-cache q8_0` |
| 长上下文 | tq3.5 | 质量中性，Google 推荐 ⭐ | `--kv-cache tq3.5 -c 65536` |
| 代码生成 | q4_0 | 成熟稳定，内存最优 | `--kv-cache q4_0` |
| 极限场景 | tq2.5 | 低比特中性能最佳 | `--kv-cache tq2.5` |
| 基准测试 | f16 | 无精度损失，参考基准 | `--kv-cache f16` |

### 实战命令集合

```bash
# 1. 日常使用（推荐）
moxing ollama run OmniCoder-9B --kv-cache q8_0 -c 65536

# 2. 长上下文场景（最佳性价比）
moxing ollama run OmniCoder-9B --kv-cache tq3.5 -c 65536

# 3. 极限显存压缩
moxing ollama run OmniCoder-9B --kv-cache tq2 -c 65536

# 4. 代码生成任务
moxing ollama run OmniCoder-9B --kv-cache q4_0 -c 32768

# 5. 基准测试（无压缩）
moxing ollama run OmniCoder-9B --kv-cache f16 -c 65536
```

---

## 九、结语

KV Cache 量化是大模型推理优化的关键技术。通过合理选择量化方法，可以在保证质量的同时降低显存占用。

### 核心结论

1. **日常使用选 q8_0** - 精度与显存的完美平衡，推荐默认使用
2. **长上下文选 tq3.5** - Google 推荐的"质量中性"点，4.57 倍压缩
3. **极限场景选 tq2.5** - 实测在低比特方法中性能最佳
4. **Apple Silicon 用户** - 内存节省效果不如独立显存明显，但仍有价值

TurboQuant 作为新技术，在保持性能的前提下提供了更高的压缩比，值得在长上下文场景中尝试！

---

*测试工具：[MoXing](https://github.com/cycleuser/MoXing)*

*作者：CycleUser*

*欢迎在评论区分享你的测试结果！*

---

## 附录：测试数据

完整测试数据已保存至 `kv_cache_results.json`，包含：
- 每种方法的加载时间
- 生成速度 (tok/s)
- TTFT (Time To First Token)
- 峰值内存占用
- 错误信息

运行测试脚本：
```bash
python scripts/quick_kv_test.py
```
